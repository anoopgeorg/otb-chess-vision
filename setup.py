from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A computer vision project to detect state of an over the board chess game.',
    author='Anoop George',
    license='',
)
